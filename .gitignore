node_modules/
dist/
out/
cache/
coverage/
.turbo/
*.tsbuildinfo

# next.js build output
.next/
next-env.d.ts

# env / secrets — NEVER commit
.env
.env.*
!.env.example

# foundry
packages/contracts/out/
packages/contracts/cache/
packages/contracts/broadcast/**/dry-run/
# forge dependencies — fetched via `pnpm --filter @nexus/contracts setup` (forge install)
packages/contracts/lib/

# logs
*.log
.DS_Store

# deployment artifacts with addresses are kept, but local runs are not
scripts/.live-state.json

# local deployment address dumps
packages/contracts/deployments/*.json

# example local env (hardcoded testnet key — never commit)
examples/**/.env.local
examples/.shared-env.local
examples/**/node_modules/
examples/**/.next/
examples/**/test-results/
examples/**/playwright-report/

# runtime secrets (generated funded player keys) — never commit, anywhere
**/players.local.json
**/players.*.local.json
**/.players.json
# next.js mono-app local env (hardcoded testnet key / bundler key) — never commit
landing/.env.local
# stray non-pnpm lockfile
**/bun.lock
examples/**/playwright/.auth/
examples/**/.pw-user-data/
examples/**/out/
examples/**/cache/
examples/**/broadcast/
examples/**/.last-run.json

# game demo videos — never commit (large, regenerated)
examples/**/demos/
**/*.webm
**/*.mp4
.vercel
